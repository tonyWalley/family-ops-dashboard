(function(){
'use strict';
const KEY='FAMILY_OPS_DASHBOARD_STATE_V1', FILE_DB='FAMILY_OPS_FILES_V1', FILE_STORE='bankFiles', $=s=>document.querySelector(s), $$=s=>[...document.querySelectorAll(s)];
const clone=o=>JSON.parse(JSON.stringify(o)), embedded=JSON.parse($('#embedded-state').textContent);
let state=load(), dirty=false, timer;
function normalizeState(x){
  x=x||{};x.settings=x.settings||{};x.tasks=Array.isArray(x.tasks)?x.tasks:[];x.transactions=Array.isArray(x.transactions)?x.transactions:[];x.unknowns=Array.isArray(x.unknowns)?x.unknowns:[];x.notes=Array.isArray(x.notes)?x.notes:[];x.budgets=Array.isArray(x.budgets)?x.budgets:[];x.bankCsvIntake=Array.isArray(x.bankCsvIntake)?x.bankCsvIntake:[];
  x.tasks.forEach(t=>{if(t.notes===undefined||t.notes===null)t.notes='';if(t.archived===undefined)t.archived=false;if(t.completedAt===undefined)t.completedAt=null;if(t.completedBy===undefined)t.completedBy=null;if(t.archivedAt===undefined)t.archivedAt=null;if(t.archivedBy===undefined)t.archivedBy=null});
  const existing=x.settings.currentCashBreakdown||{}, legacy=Number(x.settings.currentAccessibleCash||0), hasParts=['usaaAvailable','chaseArrtAvailable','physicalCash','paypalCash','otherAvailable'].some(k=>existing[k]!==undefined&&existing[k]!==null);
  x.settings.currentCashBreakdown={usaaAvailable:Number(existing.usaaAvailable||0),chaseArrtAvailable:Number(existing.chaseArrtAvailable||0),physicalCash:Number(existing.physicalCash||0),paypalCash:Number(existing.paypalCash||0),otherAvailable:Number(existing.otherAvailable||(hasParts?0:legacy)),total:Number(existing.total||legacy),asOf:existing.asOf||String(x.lastUpdated||new Date().toISOString()).slice(0,10),updatedAt:existing.updatedAt||x.lastUpdated||new Date().toISOString()};
  if(hasParts||x.settings.currentCashBreakdown.otherAvailable)x.settings.currentAccessibleCash=cashBreakdownTotal(x.settings.currentCashBreakdown);else if(x.settings.currentAccessibleCash===undefined||x.settings.currentAccessibleCash===null)x.settings.currentAccessibleCash=legacy;
  return x;
}
function load(){try{const x=JSON.parse(localStorage.getItem(KEY)||'null');if(x&&Number(x.revision)>=Number(embedded.revision))return normalizeState(x)}catch(e){}return normalizeState(clone(embedded))}
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(Number(n||0));
const cap=s=>String(s||'').replace(/\b\w/g,c=>c.toUpperCase());
const iso=()=>new Date().toISOString();
const uid=p=>p+'_'+Date.now().toString(36)+'_'+Math.random().toString(36).slice(2,7);
const updaterName=()=>$('#updatedBy')?.value.trim()||state.lastUpdatedBy||'Unknown';
function dateLabel(s){if(!s)return'';return new Date(s+'T12:00:00').toLocaleDateString('en-US',{month:'short',day:'numeric'})}
function dateTimeLabel(s){if(!s)return'';const d=new Date(s);return Number.isNaN(d.getTime())?'':d.toLocaleString()}
function owners(){const explicit=state.ownerNames||{};const found=[];[...(state.tasks||[]),...(state.transactions||[])].forEach(x=>{const o=x.owner;if(o&&o!=='Joint'&&!found.includes(o)&&o!=='ARRT Ventures')found.push(o)});return[explicit.primary||found[0]||'Tony',explicit.secondary||found[1]||'Rachel']}
function amt(t){return t.actualAmount!==null&&t.actualAmount!==''&&!Number.isNaN(Number(t.actualAmount))?Number(t.actualAmount):Number(t.amount||0)}
function txDate(t){return t.forecastDate||t.date}
function txChange(t){return t.kind==='income'?amt(t):t.kind==='transfer'?0:-amt(t)}
function included(t,mode){if(t.id==='open')return true;return !!t[mode==='downside'?'includeDownside':mode==='recovery'?'includeRecovery':'includeBase']}
function cashBreakdownTotal(b=state.settings?.currentCashBreakdown||{}){return ['usaaAvailable','chaseArrtAvailable','physicalCash','paypalCash','otherAvailable'].reduce((s,k)=>s+Number(b[k]||0),0)}
function rollingMode(){return state.settings&&state.settings.currentAccessibleCash!==null&&state.settings.currentAccessibleCash!==undefined&&!Number.isNaN(Number(state.settings.currentAccessibleCash))}
function currentCash(){return rollingMode()?Number(state.settings.currentAccessibleCash||0):Number(state.settings?.openingBalance||0)}
function currentAsOf(){return state.settings?.currentCashBreakdown?.asOf||state.settings?.asOfDate||new Date().toISOString().slice(0,10)}
function probableIncoming(days=7){if(!rollingMode())return 0;const start=new Date(currentAsOf()+'T12:00:00'),end=new Date(start);end.setDate(end.getDate()+days);return (state.transactions||[]).filter(t=>t.id!=='open'&&t.kind==='income'&&t.paymentStatus!=='paid'&&t.cashStatus==='probable').filter(t=>{const d=new Date(txDate(t)+'T12:00:00');return d>=start&&d<=end}).reduce((s,t)=>s+amt(t),0)}
function nextScheduledIncome(){return (state.transactions||[]).filter(t=>t.id!=='open'&&t.kind==='income'&&t.paymentStatus!=='paid'&&t.cashStatus==='scheduled'&&txDate(t)>=currentAsOf()).slice().sort((a,b)=>txDate(a).localeCompare(txDate(b)))[0]||null}
function safeToSpend(){const next=nextScheduledIncome(),floor=Number(state.settings?.cashFloor||0);if(!next)return{amount:currentCash()-floor,next:null,reserved:0};const cutoff=txDate(next),reserved=(state.transactions||[]).filter(t=>t.id!=='open'&&t.kind==='expense'&&t.paymentStatus==='authorized'&&txDate(t)>=currentAsOf()&&txDate(t)<cutoff).reduce((s,t)=>s+amt(t),0);return{amount:currentCash()-floor-reserved,next,reserved}}
function cashAgeHours(){const stamp=state.settings?.currentCashBreakdown?.updatedAt||state.lastUpdated;if(!stamp)return Infinity;const d=new Date(stamp);return Number.isNaN(d.getTime())?Infinity:(Date.now()-d.getTime())/36e5}
function calc(mode=state.settings.forecastMode){
  if(!rollingMode()){
    let bal=0,low=Infinity,lowDate='';
    const rows=(state.transactions||[]).filter(t=>included(t,mode)).slice().sort((a,b)=>txDate(a).localeCompare(txDate(b))||(a.kind==='income'?-1:1));
    const out=[];
    rows.forEach(t=>{const change=t.id==='open'?amt(t):txChange(t);bal=t.id==='open'?amt(t):bal+change;if(bal<low){low=bal;lowDate=txDate(t)}out.push({t,change,balance:bal})});
    if(low===Infinity){low=0;lowDate=''}
    return{rows:out,low,lowDate,end:bal,start:Number(state.settings?.openingBalance||0),asOf:state.period?.start||''};
  }
  const asOf=currentAsOf(),start=currentCash();let bal=start,low=start,lowDate=asOf;
  const rows=(state.transactions||[]).filter(t=>t.id!=='open'&&t.paymentStatus!=='paid'&&included(t,mode)&&txDate(t)>=asOf).slice().sort((a,b)=>txDate(a).localeCompare(txDate(b))||(a.kind==='income'?-1:1));
  const out=[];rows.forEach(t=>{const change=txChange(t);bal+=change;if(bal<low){low=bal;lowDate=txDate(t)}out.push({t,change,balance:bal})});return{rows:out,low,lowDate,end:bal,start,asOf};
}
function mark(){dirty=true;$('#saveStatus').textContent='Unsaved local changes';clearTimeout(timer);timer=setTimeout(()=>save(false),500)}
function touch(){state.revision=Number(state.revision||0)+1;state.lastUpdated=iso();state.lastUpdatedBy=updaterName()}
function syncSettings(){
  state.settings=state.settings||{};const b=state.settings.currentCashBreakdown||{};
  b.usaaAvailable=Number($('#cashUsaa').value||0);b.chaseArrtAvailable=Number($('#cashChase').value||0);b.physicalCash=Number($('#cashPhysical').value||0);b.paypalCash=Number($('#cashPaypal').value||0);b.otherAvailable=Number($('#cashOther').value||0);b.total=cashBreakdownTotal(b);state.settings.currentCashBreakdown=b;state.settings.currentAccessibleCash=b.total;
  state.settings.cashFloor=Number($('#cashFloor').value||0);state.settings.forecastMode=$('#forecastMode').value;state.lastUpdatedBy=updaterName();
}
function stampCashUpdate(){state.settings.currentCashBreakdown.updatedAt=iso();state.settings.currentCashBreakdown.asOf=new Date().toISOString().slice(0,10)}
function save(increment=true){syncSettings();if(increment)touch();localStorage.setItem(KEY,JSON.stringify(state));dirty=false;renderStatus();if(increment)renderAll()}
function renderStatus(){const d=new Date(state.lastUpdated||Date.now());$('#saveStatus').textContent='Saved on this device';$('#revisionStatus').textContent='Revision '+Number(state.revision||0);$('#updatedStatus').textContent='Updated '+d.toLocaleString();$('#modeStatus').textContent=cap(state.settings.forecastMode)+' forecast'}
function renderSettings(){const b=state.settings.currentCashBreakdown||{};$('#currentCash').value=currentCash().toFixed(2);$('#cashUsaa').value=Number(b.usaaAvailable||0).toFixed(2);$('#cashChase').value=Number(b.chaseArrtAvailable||0).toFixed(2);$('#cashPhysical').value=Number(b.physicalCash||0).toFixed(2);$('#cashPaypal').value=Number(b.paypalCash||0).toFixed(2);$('#cashOther').value=Number(b.otherAvailable||0).toFixed(2);$('#cashFloor').value=Number(state.settings.cashFloor||0);$('#forecastMode').value=state.settings.forecastMode||'base';$('#updatedBy').value=state.lastUpdatedBy==='Not set'?'':state.lastUpdatedBy||'';$('#cashUpdatedLabel').textContent=`Balances updated ${dateTimeLabel(b.updatedAt)||'unknown'}`}
function renderOverview(){
  const c=calc(),floor=Number(state.settings.cashFloor||0),activeTasks=(state.tasks||[]).filter(t=>!t.archived),done=activeTasks.filter(t=>t.done).length,btotal=(state.budgets||[]).reduce((s,b)=>s+Number(b.amount||0),0),prob=probableIncoming(7),safe=safeToSpend(),age=cashAgeHours();
  const cashSub=`USAA + Chase/ARRT + cash + PayPal${prob?` | Probable next 7 days: ${money(prob)}`:''}`,safeLabel=safe.next?`Safe to spend until ${dateLabel(txDate(safe.next))}`:'Cash above floor',safeSmall=safe.next?`${money(safe.reserved)} authorized before next scheduled income`:'No scheduled income found; cash floor only';
  $('#kpis').innerHTML=`<div class="card"><div class="label">Current accessible cash</div><div class="value">${money(c.start)}</div><div class="small">${cashSub}</div></div><div class="card"><div class="label">${safeLabel}</div><div class="value ${safe.amount<0?'dangertext':'goodtext'}">${money(safe.amount)}</div><div class="small">${safeSmall}</div></div><div class="card"><div class="label">Lowest projected cash</div><div class="value ${c.low<floor?'dangertext':'goodtext'}">${money(c.low)}</div><div class="small">${dateLabel(c.lowDate)} in ${cap(state.settings.forecastMode)} mode</div></div><div class="card"><div class="label">End-of-period cash</div><div class="value">${money(c.end)}</div><div class="small">Before unmodeled items</div></div><div class="card"><div class="label">Task progress</div><div class="value">${done}/${activeTasks.length}</div><div class="small">Archived tasks excluded</div></div><div class="card"><div class="label">Household budget</div><div class="value">${money(btotal)}</div><div class="small">Current working variable budget</div></div>`;
  const warnings=[];if(age>36)warnings.push(`<div class="callout warn"><strong>Cash data may be stale:</strong> balances were last refreshed ${Math.round(age)} hours ago. Update account balances before authorizing a material payment.</div>`);warnings.push(c.low<floor?`<div class="callout danger"><strong>Cash-floor warning:</strong> The forecast reaches ${money(c.low)} on ${dateLabel(c.lowDate)}, below the ${money(floor)} floor.</div>`:`<div class="callout good"><strong>Forecast check:</strong> The selected plan remains above the ${money(floor)} cash floor. Probable income is shown separately unless included in the selected scenario.</div>`);$('#warningBox').innerHTML=warnings.join('');
  const upcoming=(state.transactions||[]).filter(t=>t.id!=='open'&&t.paymentStatus!=='paid'&&included(t,state.settings.forecastMode)&&(!rollingMode()||txDate(t)>=currentAsOf())).sort((a,b)=>txDate(a).localeCompare(txDate(b))).slice(0,6);$('#next7').innerHTML=upcoming.map(t=>`<div class="task compact-task"><span class="tag ${esc(t.paymentStatus)}">${esc(t.paymentStatus)}</span><div><strong>${dateLabel(txDate(t))} - ${esc(t.name)}</strong><div class="small">${esc(t.owner)} | ${t.kind==='transfer'?'Transfer ':''}${money(amt(t))}</div></div><span></span></div>`).join('')||'<p>No upcoming items. Import the private JSON to load the plan.</p>';
  $('#openItems').innerHTML=(state.unknowns||[]).slice(0,8).map(u=>`<div class="task compact-task"><span class="tag unknown">OPEN</span><div><strong>${esc(u.item)} - ${esc(u.amount)}</strong><div class="small">${esc(u.owner)}: ${esc(u.action)}</div></div><span></span></div>`).join('')||'<p>No unconfirmed items recorded.</p>';
  const order={authorized:0,conditional:1,hold:2,planned:3,unknown:4},nextTx=(state.transactions||[]).filter(t=>t.id!=='open'&&t.paymentStatus!=='paid'&&txDate(t)>=currentAsOf()).sort((x,y)=>(order[x.paymentStatus]??9)-(order[y.paymentStatus]??9)||txDate(x).localeCompare(txDate(y))).slice(0,3),nextTasks=(state.tasks||[]).filter(t=>!t.archived&&!t.done).slice(0,3),rows=[];nextTx.forEach(t=>rows.push(`<div class="next-row"><span class="tag ${esc(t.paymentStatus)}">${cap(t.paymentStatus)}</span><div><strong>${dateLabel(txDate(t))}: ${esc(t.name)}</strong><div class="small">${esc(t.owner)} | ${money(amt(t))}</div></div></div>`));nextTasks.forEach(t=>rows.push(`<div class="next-row"><span class="tag planned">TASK</span><div><strong>${esc(t.text)}</strong><div class="small">${esc(t.owner)}${t.notes?` | ${esc(t.notes)}`:''}</div></div></div>`));$('#todayNext').innerHTML=rows.join('')||'<p class="small">Nothing urgent is currently modeled.</p>';
}
function calendarEvent(t){const prefix=t.kind==='income'?'+':t.kind==='transfer'?'↔ ':'-';return`<div class="event ${t.paymentStatus==='paid'?'paid':t.paymentStatus==='conditional'?'conditional':t.paymentStatus==='hold'?'hold':t.kind}" data-tx="${esc(t.id)}"><span>${esc(t.name)}</span> <strong>${prefix}${money(amt(t))}</strong></div>`}
function bindCalendarJumps(root){root.querySelectorAll('[data-tx]').forEach(e=>e.onclick=()=>{tab('cashplan');setTimeout(()=>document.querySelector(`[data-row="${CSS.escape(e.dataset.tx)}"],[data-card="${CSS.escape(e.dataset.tx)}"]`)?.scrollIntoView({behavior:'smooth',block:'center'}),50)})}
function renderCalendar(){const desktop=$('#calendarGrid'),agenda=$('#calendarAgenda'),start=new Date((state.period?.start||new Date().toISOString().slice(0,7)+'-01')+'T12:00:00'),y=start.getFullYear(),m=start.getMonth(),first=new Date(y,m,1),offset=first.getDay(),cells=Math.ceil((offset+new Date(y,m+1,0).getDate())/7)*7,c=calc(),cash={};c.rows.forEach(r=>cash[txDate(r.t)]=r.balance);let h=['Sun','Mon','Tue','Wed','Thu','Fri','Sat'].map(x=>`<div class="calhead">${x}</div>`).join('');for(let i=0;i<cells;i++){const d=new Date(y,m,1-offset+i),ds=d.toISOString().slice(0,10),outside=d.getMonth()!==m;let ev=(state.transactions||[]).filter(t=>t.id!=='open'&&txDate(t)===ds).map(calendarEvent).join('');if(cash[ds]!==undefined)ev+=`<div class="event cash">After day: ${money(cash[ds])} available</div>`;h+=`<div class="calday ${outside?'outside':''}"><div class="daynum">${d.toLocaleDateString('en-US',{month:'short',day:'numeric'})}</div>${ev}</div>`}desktop.innerHTML=h;let a='';const days=new Date(y,m+1,0).getDate();for(let day=1;day<=days;day++){const d=new Date(y,m,day),ds=d.toISOString().slice(0,10),items=(state.transactions||[]).filter(t=>t.id!=='open'&&txDate(t)===ds);if(!items.length&&cash[ds]===undefined)continue;a+=`<div class="agenda-day"><div class="agenda-date"><strong>${d.toLocaleDateString('en-US',{weekday:'short'})}</strong><span>${d.toLocaleDateString('en-US',{month:'short',day:'numeric'})}</span></div><div class="agenda-events">${items.map(calendarEvent).join('')}${cash[ds]!==undefined?`<div class="agenda-cash">After day <strong>${money(cash[ds])}</strong> available</div>`:''}</div></div>`}agenda.innerHTML=a||'<p class="small">No calendar activity for this period.</p>';bindCalendarJumps(desktop);bindCalendarJumps(agenda)}
function txItems(){return (state.transactions||[]).filter(t=>t.id!=='open')}
function bindTxEditors(root){root.querySelectorAll('[data-f]').forEach(el=>el.onchange=e=>{const t=state.transactions.find(x=>x.id===e.target.dataset.id),f=e.target.dataset.f;if(!t||!f)return;t[f]=e.target.type==='checkbox'?e.target.checked:['amount','actualAmount'].includes(f)?(e.target.value===''?null:Number(e.target.value)):e.target.value;if(f==='paymentStatus'&&t[f]==='paid'&&t.actualAmount===null)t.actualAmount=t.amount;mark();renderOverview();renderCalendar();renderRunning();if(root.id==='cashCards')renderCashCards()})}
function renderCashCards(){const box=$('#cashCards'),[a,b]=owners(),opts=[a,b,'Joint'],statuses=['authorized','conditional','hold','planned','unknown','paid'];box.innerHTML=txItems().slice().sort((a,b)=>txDate(a).localeCompare(txDate(b))).map(t=>{const prefix=t.kind==='income'?'+':t.kind==='transfer'?'↔ ':'-',amountClass=t.kind==='income'?'money-positive':t.kind==='transfer'?'money-neutral':'money-negative';return`<article class="cashcard" data-card="${esc(t.id)}"><div class="cashcard-top"><div><div class="cashcard-date">${dateLabel(txDate(t))}</div><strong>${esc(t.name)}</strong><div class="small">${esc(t.owner)}${t.forecastDate&&t.forecastDate!==t.date?` | due ${dateLabel(t.date)}`:''}</div></div><div class="cashcard-amount ${amountClass}">${prefix}${money(amt(t))}</div></div><div class="cashcard-actions"><span class="tag ${esc(t.paymentStatus)}">${cap(t.paymentStatus)}</span>${t.paymentStatus!=='paid'?`<button class="btn good markpaid" data-id="${esc(t.id)}">Mark paid</button>`:''}</div><details><summary>Details / edit</summary><div class="cashcard-fields"><label>Date<input type="date" data-f="date" data-id="${esc(t.id)}" value="${esc(t.date)}"></label><label>Owner<select data-f="owner" data-id="${esc(t.id)}">${opts.map(x=>`<option ${t.owner===x?'selected':''}>${esc(x)}</option>`).join('')}</select></label><label>Status<select data-f="paymentStatus" data-id="${esc(t.id)}">${statuses.map(x=>`<option value="${x}" ${t.paymentStatus===x?'selected':''}>${cap(x)}</option>`).join('')}</select></label><label>Planned<input type="number" step="0.01" data-f="amount" data-id="${esc(t.id)}" value="${Number(t.amount||0).toFixed(2)}"></label><label>Actual<input type="number" step="0.01" data-f="actualAmount" data-id="${esc(t.id)}" value="${t.actualAmount===null?'':Number(t.actualAmount).toFixed(2)}"></label><label class="cashcard-notes">Notes<input data-f="notes" data-id="${esc(t.id)}" value="${esc(t.notes||'')}"></label></div><div class="scenario-row"><label><input type="checkbox" data-f="includeBase" data-id="${esc(t.id)}" ${t.includeBase?'checked':''}> Base</label><label><input type="checkbox" data-f="includeDownside" data-id="${esc(t.id)}" ${t.includeDownside?'checked':''}> Downside</label><label><input type="checkbox" data-f="includeRecovery" data-id="${esc(t.id)}" ${t.includeRecovery?'checked':''}> Recovery</label></div></details></article>`}).join('')||'<p>No cash-plan items.</p>';bindTxEditors(box);box.querySelectorAll('.markpaid').forEach(btn=>btn.onclick=()=>{const t=state.transactions.find(x=>x.id===btn.dataset.id);if(!t)return;t.paymentStatus='paid';if(t.actualAmount===null)t.actualAmount=t.amount;mark();renderAll()})}
function renderTransactions(){const [a,b]=owners(),opts=[a,b,'Joint'],statuses=['authorized','conditional','hold','planned','unknown','paid'],items=txItems();$('#txBody').innerHTML=items.map(t=>`<tr data-row="${esc(t.id)}"><td><input type="date" data-f="date" data-id="${esc(t.id)}" value="${esc(t.date)}"></td><td><input data-f="name" data-id="${esc(t.id)}" value="${esc(t.name)}"></td><td><select data-f="owner" data-id="${esc(t.id)}">${opts.map(x=>`<option ${t.owner===x?'selected':''}>${esc(x)}</option>`).join('')}</select></td><td><select data-f="kind" data-id="${esc(t.id)}"><option value="expense" ${t.kind==='expense'?'selected':''}>Expense</option><option value="income" ${t.kind==='income'?'selected':''}>Income</option><option value="transfer" ${t.kind==='transfer'?'selected':''}>Transfer</option></select></td><td><select data-f="paymentStatus" data-id="${esc(t.id)}">${statuses.map(x=>`<option value="${x}" ${t.paymentStatus===x?'selected':''}>${cap(x)}</option>`).join('')}</select></td><td><input class="num" type="number" step="0.01" data-f="amount" data-id="${esc(t.id)}" value="${Number(t.amount||0).toFixed(2)}"></td><td><input class="num" type="number" step="0.01" data-f="actualAmount" data-id="${esc(t.id)}" value="${t.actualAmount===null?'':Number(t.actualAmount).toFixed(2)}"></td>${['includeBase','includeDownside','includeRecovery'].map(f=>`<td><input type="checkbox" data-f="${f}" data-id="${esc(t.id)}" ${t[f]?'checked':''}></td>`).join('')}<td><input data-f="notes" data-id="${esc(t.id)}" value="${esc(t.notes||'')}"></td><td><button class="btn danger deltx" data-id="${esc(t.id)}">x</button></td></tr>`).join('');bindTxEditors($('#txBody'));$('#txBody').querySelectorAll('.deltx').forEach(x=>x.onclick=()=>{if(confirm('Delete this cash-plan item?')){state.transactions=state.transactions.filter(t=>t.id!==x.dataset.id);mark();renderAll()}});renderCashCards()}
function renderRunning(){$('#runningBody').innerHTML=calc().rows.map(r=>`<tr><td>${dateLabel(txDate(r.t))}</td><td>${esc(r.t.name)}</td><td><span class="tag ${esc(r.t.paymentStatus)}">${esc(r.t.paymentStatus)}</span></td><td class="num ${r.change>0?'money-positive':r.change<0?'money-negative':'money-neutral'}">${r.change>0?'+':''}${money(r.change)}</td><td class="num">${money(r.balance)}</td></tr>`).join('')||`<tr><td>${dateLabel(currentAsOf())}</td><td>Current accessible cash</td><td><span class="tag paid">Current</span></td><td class="num">-</td><td class="num">${money(currentCash())}</td></tr>`}
function renderBudgets(){$('#budgetRows').innerHTML=(state.budgets||[]).map(b=>{const rem=Number(b.amount||0)-Number(b.spent||0);return`<div class="budgetrow" data-budget="${esc(b.id)}"><div class="wide"><strong>${esc(b.name)}</strong><div class="small">${esc(b.note||'')}</div></div><div class="field"><label>Budget</label><input type="number" step="0.01" data-bf="amount" value="${Number(b.amount||0).toFixed(2)}"></div><div class="field"><label>Spent</label><input type="number" step="0.01" data-bf="spent" value="${Number(b.spent||0).toFixed(2)}"></div><div><strong class="${rem<0?'dangertext':'goodtext'}">${money(rem)} remaining</strong></div></div>`}).join('')||'<p>No budgets loaded.</p>';$('#budgetRows').querySelectorAll('input').forEach(el=>el.onchange=e=>{const row=e.target.closest('[data-budget]'),b=state.budgets.find(x=>x.id===row.dataset.budget);b[e.target.dataset.bf]=Number(e.target.value||0);mark();renderBudgets();renderOverview()})}
function renderTasks(){const [a,b]=owners();$('#owner1Tab').textContent=a;$('#owner2Tab').textContent=b;$('#owner1Title').textContent=a+"'s To-Do List";$('#owner2Title').textContent=b+"'s To-Do List";renderTaskOwner(a,'#owner1Tasks','#owner1ArchivedTasks','#owner1Progress','#owner1ProgressText','#owner1ArchiveCount');renderTaskOwner(b,'#owner2Tasks','#owner2ArchivedTasks','#owner2Progress','#owner2ProgressText','#owner2ArchiveCount')}
function renderTaskOwner(owner,activeSel,archiveSel,progressSel,textSel,countSel){const all=(state.tasks||[]).filter(t=>t.owner===owner),list=all.filter(t=>!t.archived),archived=all.filter(t=>t.archived),done=list.filter(t=>t.done).length,total=list.length,p=total?Math.round(done/total*100):0,box=$(activeSel),archiveBox=$(archiveSel);$(progressSel).style.width=p+'%';$(textSel).textContent=`${done} of ${total} active tasks complete (${p}%)`;$(countSel).textContent=archived.length?`(${archived.length})`:'';box.innerHTML=list.map(t=>`<div class="task task-with-note ${t.done?'done':''}" data-task="${esc(t.id)}"><input class="task-check" type="checkbox" ${t.done?'checked':''}><div class="taskbody"><div class="tasktext">${esc(t.text)}</div>${t.done&&t.completedAt?`<div class="task-meta">Completed ${dateTimeLabel(t.completedAt)} by ${esc(t.completedBy||'Unknown')}</div>`:''}<label class="task-note-label">Notes / update<textarea class="task-note" placeholder="Add details, outcome, phone call notes, confirmation number, or next step...">${esc(t.notes||'')}</textarea></label></div><div class="task-actions"><button class="task-archive btn" title="Archive task">Archive</button><button class="task-delete" title="Delete task">x</button></div></div>`).join('')||'<p class="small">No active tasks.</p>';archiveBox.innerHTML=archived.slice().sort((x,y)=>String(y.archivedAt||'').localeCompare(String(x.archivedAt||''))).map(t=>`<div class="archived-task" data-task="${esc(t.id)}"><div><strong>${esc(t.text)}</strong>${t.notes?`<div class="small">${esc(t.notes)}</div>`:''}<div class="task-meta">Archived ${dateTimeLabel(t.archivedAt)} by ${esc(t.archivedBy||'Unknown')}</div></div><div class="actions"><button class="btn restore-task">Restore</button><button class="btn danger delete-archived">Delete</button></div></div>`).join('')||'<p class="small">No archived tasks.</p>';box.querySelectorAll('.task').forEach(row=>{const task=()=>state.tasks.find(x=>x.id===row.dataset.task);row.querySelector('.task-check').onchange=e=>{const t=task();if(!t)return;t.done=e.target.checked;if(t.done){t.completedAt=iso();t.completedBy=updaterName()}else{t.completedAt=null;t.completedBy=null}mark();renderTasks();renderOverview()};row.querySelector('.task-note').oninput=e=>{const t=task();if(!t)return;t.notes=e.target.value;mark()};row.querySelector('.task-archive').onclick=()=>{const t=task();if(!t)return;t.archived=true;t.archivedAt=iso();t.archivedBy=updaterName();mark();renderTasks();renderOverview()};row.querySelector('.task-delete').onclick=()=>{if(confirm('Delete this task?')){state.tasks=state.tasks.filter(x=>x.id!==row.dataset.task);mark();renderTasks();renderOverview()}}});archiveBox.querySelectorAll('.archived-task').forEach(row=>{row.querySelector('.restore-task').onclick=()=>{const t=state.tasks.find(x=>x.id===row.dataset.task);if(!t)return;t.archived=false;t.archivedAt=null;t.archivedBy=null;mark();renderTasks();renderOverview()};row.querySelector('.delete-archived').onclick=()=>{if(confirm('Permanently delete this archived task?')){state.tasks=state.tasks.filter(x=>x.id!==row.dataset.task);mark();renderTasks();renderOverview()}}})}
function renderNotes(){$('#notesList').innerHTML=(state.notes||[]).slice().sort((a,b)=>String(b.time).localeCompare(String(a.time))).map(n=>`<div class="noteitem"><strong>${esc(n.author)}</strong> <span class="small">${new Date(n.time).toLocaleString()}</span><div>${esc(n.text)}</div></div>`).join('')||'<p>No updates yet.</p>'}
function renderUnknowns(){$('#unknownBody').innerHTML=(state.unknowns||[]).map(u=>`<tr><td>${esc(u.item)}</td><td>${esc(u.amount)}</td><td>${esc(u.owner)}</td><td>${esc(u.action)}</td></tr>`).join('')}
function openFileDb(){return new Promise((resolve,reject)=>{if(!('indexedDB'in window))return reject(Error('This browser does not support local file staging.'));const req=indexedDB.open(FILE_DB,1);req.onupgradeneeded=()=>{const db=req.result;if(!db.objectStoreNames.contains(FILE_STORE))db.createObjectStore(FILE_STORE,{keyPath:'id'})};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error)})}
async function storeBankFile(id,file){const db=await openFileDb();return new Promise((resolve,reject)=>{const tx=db.transaction(FILE_STORE,'readwrite');tx.objectStore(FILE_STORE).put({id,name:file.name,type:file.type||'text/csv',blob:file});tx.oncomplete=()=>{db.close();resolve()};tx.onerror=()=>{db.close();reject(tx.error)}})}
async function getBankFile(id){const db=await openFileDb();return new Promise((resolve,reject)=>{const tx=db.transaction(FILE_STORE,'readonly'),req=tx.objectStore(FILE_STORE).get(id);req.onsuccess=()=>{db.close();resolve(req.result||null)};req.onerror=()=>{db.close();reject(req.error)}})}
async function deleteBankFile(id){try{const db=await openFileDb();await new Promise((resolve,reject)=>{const tx=db.transaction(FILE_STORE,'readwrite');tx.objectStore(FILE_STORE).delete(id);tx.oncomplete=()=>resolve();tx.onerror=()=>reject(tx.error)});db.close()}catch(e){}}
function formatBytes(n){const x=Number(n||0);if(x<1024)return`${x} B`;if(x<1048576)return`${(x/1024).toFixed(1)} KB`;return`${(x/1048576).toFixed(1)} MB`}
function standardCsvName(meta){const base=String(meta.fileName||'transactions.csv').replace(/\.csv$/i,'').replace(/[^a-z0-9_-]+/gi,'_').replace(/^_+|_+$/g,'').slice(0,50)||'transactions',stamp=String(meta.stagedAt||new Date().toISOString()).slice(0,10).replace(/-/g,'');return`WFOS_${String(meta.source||'BANK').toUpperCase()}_${stamp}_${base}.csv`}
function renderBankCsv(){const box=$('#bankCsvList'),items=(state.bankCsvIntake||[]).slice().sort((a,b)=>String(b.stagedAt).localeCompare(String(a.stagedAt)));box.innerHTML=items.map(i=>`<div class="csv-item" data-csv="${esc(i.id)}"><div><span class="tag planned">${esc(i.source)}</span> <strong>${esc(i.fileName)}</strong><div class="small">${formatBytes(i.size)} | staged ${dateTimeLabel(i.stagedAt)} by ${esc(i.stagedBy||'Unknown')}</div><div class="small">Status: pending ChatGPT review/categorization. File content is stored only on the device where it was attached.</div></div><div class="actions"><button class="btn primary share-csv">Share original CSV</button><button class="btn danger remove-csv">Remove</button></div></div>`).join('')||'<p class="small">No bank CSV files staged yet.</p>';box.querySelectorAll('.csv-item').forEach(row=>{row.querySelector('.share-csv').onclick=()=>shareBankCsv(row.dataset.csv);row.querySelector('.remove-csv').onclick=()=>removeBankCsv(row.dataset.csv)})}
async function stageBankCsv(){const source=$('#bankCsvSource').value,files=[...($('#bankCsvFile').files||[])];if(!files.length)return alert('Choose at least one CSV file.');for(const file of files){if(!/\.csv$/i.test(file.name)&&!String(file.type||'').toLowerCase().includes('csv')){alert(`${file.name} does not look like a CSV file and was skipped.`);continue}const duplicate=(state.bankCsvIntake||[]).find(i=>i.source===source&&i.fileName===file.name&&Number(i.size)===Number(file.size)&&Number(i.lastModified)===Number(file.lastModified));const id=duplicate?.id||uid('csv');try{await storeBankFile(id,file)}catch(e){alert(`Could not store ${file.name} on this device: ${e.message}`);continue}const meta={id,source,fileName:file.name,size:file.size,lastModified:file.lastModified,stagedAt:iso(),stagedBy:updaterName(),reviewStatus:'pending_chatgpt_review'};if(duplicate)Object.assign(duplicate,meta);else state.bankCsvIntake.push(meta)}$('#bankCsvFile').value='';mark();renderBankCsv()}
async function shareBankCsv(id){const meta=(state.bankCsvIntake||[]).find(i=>i.id===id);if(!meta)return;let rec;try{rec=await getBankFile(id)}catch(e){}if(!rec||!rec.blob)return alert('The CSV metadata is present, but the original file is not stored on this device. Reattach the CSV here before sharing it.');const file=new File([rec.blob],standardCsvName(meta),{type:rec.type||'text/csv'});if(navigator.share&&navigator.canShare?.({files:[file]})){try{await navigator.share({title:`${meta.source} bank CSV`,files:[file]});return}catch(e){if(e?.name==='AbortError')return}}blobDownload(rec.blob,standardCsvName(meta))}
async function shareAllBankCsv(){const files=[];for(const meta of state.bankCsvIntake||[]){try{const rec=await getBankFile(meta.id);if(rec?.blob)files.push(new File([rec.blob],standardCsvName(meta),{type:rec.type||'text/csv'}))}catch(e){}}if(!files.length)return alert('No original CSV files are stored on this device.');if(navigator.share&&navigator.canShare?.({files})){try{await navigator.share({title:'WFOS bank CSV intake',files});return}catch(e){if(e?.name==='AbortError')return}}files.forEach(f=>blobDownload(f,f.name))}
async function removeBankCsv(id){if(!confirm('Remove this staged CSV from this device and the intake list?'))return;await deleteBankFile(id);state.bankCsvIntake=state.bankCsvIntake.filter(i=>i.id!==id);mark();renderBankCsv()}
function renderAll(){renderSettings();renderOverview();renderCalendar();renderTransactions();renderRunning();renderBudgets();renderTasks();renderNotes();renderUnknowns();renderBankCsv();renderStatus()}
function addTx(kind='expense'){const d=prompt('Date (YYYY-MM-DD)',new Date().toISOString().slice(0,10)),name=d&&prompt(kind==='transfer'?'Transfer description':'Item name');if(!name)return;const amount=Number(prompt('Amount','0')||0),[o]=owners();state.transactions.push({id:uid('tx'),date:d,name,owner:o,category:kind==='transfer'?'Transfer':'Other',kind,amount,actualAmount:null,cashStatus:'',paymentStatus:'unknown',includeBase:true,includeDownside:true,includeRecovery:true,autopay:false,notes:kind==='transfer'?'Internal transfer; should not be counted as spending.':''});mark();renderAll()}
function addTask(owner){const text=prompt(`New task for ${owner}`);if(!text)return;const notes=prompt('Optional notes / details','')||'';state.tasks=state.tasks||[];state.tasks.push({id:uid('task'),owner,text,notes,done:false,archived:false,completedAt:null,completedBy:null,archivedAt:null,archivedBy:null});mark();renderTasks();renderOverview()}
function addNote(){const text=$('#noteText').value.trim();if(!text)return;state.notes=state.notes||[];state.notes.push({id:uid('note'),time:iso(),author:$('#noteAuthor').value.trim()||state.lastUpdatedBy||'Unknown',text});$('#noteText').value='';mark();renderNotes()}
function blobDownload(blob,name){const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove()},500)}
function exportState(share=false){touch();localStorage.setItem(KEY,JSON.stringify(state));renderAll();const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),file=new File([blob],'WFOS_CURRENT_STATE.json',{type:'application/json'});if(share&&navigator.share&&navigator.canShare?.({files:[file]})){navigator.share({title:'Family Ops Current State',files:[file]}).catch(()=>{});return}blobDownload(blob,'WFOS_CURRENT_STATE.json')}
async function shareStateText(){touch();localStorage.setItem(KEY,JSON.stringify(state));renderAll();const text=JSON.stringify(state,null,2);if(navigator.share){try{await navigator.share({title:'Family Ops Current State',text});return}catch(e){if(e&&e.name==='AbortError')return}}try{await navigator.clipboard.writeText(text);alert('JSON copied as text. Paste it into Messages.')}catch(e){alert('Could not open text sharing. Use Download JSON or copy the JSON from another device.')}}
function cleanJsonText(text){return String(text||'').trim().replace(/^```(?:json)?\s*/i,'').replace(/\s*```$/,'').trim()}
function acceptImportedState(x,sourceLabel){if(!x||!x.schemaVersion||!Array.isArray(x.transactions))throw Error('Not a compatible Family Ops state');x=normalizeState(x);const l=Number(state.revision||0),n=Number(x.revision||0);if(n<=l&&!confirm(`${sourceLabel} is revision ${n}; this device has revision ${l}. Replace local state?`))return false;state=x;localStorage.setItem(KEY,JSON.stringify(state));dirty=false;renderAll();alert(`Imported revision ${state.revision||0}. Bank CSV metadata may transfer, but original CSV files remain only on the device where they were attached.`);return true}
function importText(raw,sourceLabel='Pasted JSON'){try{const cleaned=cleanJsonText(raw);if(!cleaned)throw Error('Nothing was pasted');const x=JSON.parse(cleaned);return acceptImportedState(x,sourceLabel)}catch(e){alert('Could not import: '+e.message);return false}}
function importState(file){const r=new FileReader();r.onload=()=>{try{const x=JSON.parse(r.result);acceptImportedState(x,'Incoming file')}catch(e){alert('Could not import: '+e.message)}};r.readAsText(file)}
function summary(){const c=calc(),safe=safeToSpend(),[a,b]=owners(),items=(state.transactions||[]).filter(t=>t.id!=='open'&&t.paymentStatus!=='paid'&&included(t,state.settings.forecastMode)&&(!rollingMode()||txDate(t)>=currentAsOf())).sort((x,y)=>txDate(x).localeCompare(txDate(y))).slice(0,18),tasks=o=>(state.tasks||[]).filter(t=>t.owner===o&&!t.done&&!t.archived).slice(0,12),taskList=o=>tasks(o).map(t=>`<li><strong>${esc(t.text)}</strong>${t.notes?`<div><em>Note:</em> ${esc(t.notes)}</div>`:''}</li>`).join('')||'<li>No open tasks.</li>';return`<h1>Family Operations Update</h1><p><strong>Updated:</strong> ${new Date(state.lastUpdated).toLocaleString()} by ${esc(state.lastUpdatedBy)}</p><h2>Cash Outlook</h2><ul><li>Current accessible cash: <strong>${money(c.start)}</strong></li><li>Safe to spend${safe.next?` until ${dateLabel(txDate(safe.next))}`:''}: <strong>${money(safe.amount)}</strong></li>${probableIncoming(7)?`<li>Probable incoming next 7 days: <strong>${money(probableIncoming(7))}</strong></li>`:''}<li>Low point: <strong>${money(c.low)}</strong> on ${dateLabel(c.lowDate)}</li><li>End cash: <strong>${money(c.end)}</strong></li></ul><h2>Upcoming Items</h2><ul>${items.map(t=>`<li>${dateLabel(txDate(t))}: <strong>${esc(t.name)}</strong> ${t.kind==='income'?'+':t.kind==='transfer'?'transfer ': '-'}${money(amt(t))}</li>`).join('')}</ul><h2>${esc(a)}</h2><ul>${taskList(a)}</ul><h2>${esc(b)}</h2><ul>${taskList(b)}</ul>`}
function showSummary(){$('#summaryContent').innerHTML=summary();$('#summaryModal').classList.remove('hidden')}
async function copySummary(rich){const html=summary(),div=document.createElement('div');div.innerHTML=html;const text=div.innerText;try{if(rich&&window.ClipboardItem)await navigator.clipboard.write([new ClipboardItem({'text/html':new Blob([html],{type:'text/html'}),'text/plain':new Blob([text],{type:'text/plain'})})]);else await navigator.clipboard.writeText(text);alert('Summary copied.')}catch(e){showSummary();const range=document.createRange();range.selectNodeContents($('#summaryContent'));const sel=getSelection();sel.removeAllRanges();sel.addRange(range)}}
function tab(id){$$('.tab').forEach(x=>x.classList.toggle('active',x.dataset.tab===id));$$('.section').forEach(x=>x.classList.toggle('active',x.id===id));scrollTo({top:0,behavior:'smooth'})}
$$('.tab').forEach(x=>x.onclick=()=>tab(x.dataset.tab));
$('#saveBtn').onclick=()=>save(true);$('#shareTextBtn').onclick=shareStateText;$('#shareBtn').onclick=()=>exportState(true);$('#exportBtn').onclick=()=>exportState(false);$('#copyBtn').onclick=showSummary;$('#closeSummary').onclick=()=>$('#summaryModal').classList.add('hidden');$('#copyRichBtn').onclick=()=>copySummary(true);$('#copyPlainBtn').onclick=()=>copySummary(false);$('#addExpenseBtn').onclick=()=>addTx('expense');$('#addIncomeBtn').onclick=()=>addTx('income');$('#addTransferBtn').onclick=()=>addTx('transfer');$('#sortTxBtn').onclick=()=>{state.transactions.sort((a,b)=>txDate(a).localeCompare(txDate(b)));mark();renderTransactions();renderRunning()};$('#addOwner1TaskBtn').onclick=()=>addTask(owners()[0]);$('#addOwner2TaskBtn').onclick=()=>addTask(owners()[1]);$('#addNoteBtn').onclick=addNote;$('#stageBankCsvBtn').onclick=stageBankCsv;$('#shareAllCsvBtn').onclick=shareAllBankCsv;$('#resetBtn').onclick=()=>{if(confirm('Clear this browser copy and return to the blank application shell?')){localStorage.removeItem(KEY);state=normalizeState(clone(embedded));renderAll()}};$('#importFile').onchange=e=>{if(e.target.files[0])importState(e.target.files[0]);e.target.value=''};$('#importPasteBtn').onclick=()=>{if(importText($('#pasteJson').value))$('#pasteJson').value=''};$('#clearPasteBtn').onclick=()=>{$('#pasteJson').value=''};
['cashUsaa','cashChase','cashPhysical','cashPaypal','cashOther'].forEach(id=>$('#'+id).onchange=()=>{syncSettings();stampCashUpdate();mark();renderSettings();renderOverview();renderCalendar();renderRunning();renderStatus()});['cashFloor','forecastMode','updatedBy'].forEach(id=>$('#'+id).onchange=()=>{syncSettings();mark();renderOverview();renderCalendar();renderRunning();renderStatus()});addEventListener('beforeunload',()=>{if(dirty){syncSettings();localStorage.setItem(KEY,JSON.stringify(state))}});addEventListener('resize',()=>renderCalendar());renderAll();
})();
